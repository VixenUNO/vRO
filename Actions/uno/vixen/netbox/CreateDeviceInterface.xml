<?xml version='1.0' encoding='UTF-8'?>
<dunes-script-module name="CreateDeviceInterface" result-type="string" api-version="6.0.0" id="a2d05ad8-a0eb-48b0-92be-44d7cab3d2a3" version="0.0.0" category-name="uno.vixen.netbox">
  <param n="DeviceID" t="string"/>
  <param n="token" t="string"/>
  <param n="att_resthost" t="REST:RESTHost"/>
  <param n="interfacename" t="string"/>
  <script encoded="false"><![CDATA[var operationUrl = ("/api/dcim/interfaces/");
var requestType = "POST";
var retrycount = 5;

var body = {
    "device": DeviceID,
    "name": interfacename,
    "type": "virtual",
    "enabled": true
};

var requestContent = JSON.stringify(body);

System.log (requestContent);

var requestContentType = "application/json";
var request = att_resthost.createRequest(requestType, operationUrl, requestContent);
request.contentType = requestContentType;
request.setHeader("Authorization","Token " + token);
System.log(request.headers);
for (var i=1;i<retrycount+1;i++) {
    try {
        response = request.execute();
    } catch (e) {
        System.error(e);
        System.sleep(10000);
        continue;
    }
    strresponse = response.contentAsString;
    System.log("Response Status Code: " + response.statusCode);
    System.log("Content As String: " + strresponse);
    break;
}

var str = JSON.parse(response.contentAsString);

var interfaceid = str.id;

return interfaceid;]]></script>
</dunes-script-module>